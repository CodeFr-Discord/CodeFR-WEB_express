import './page-style.css';

async function connexionSettings() {
    let state = null;

    if(state === null) {
        // register or login
    } else {
        // already login
    }
}

const Connexion = () => {
    return(
        <div className=''>
            <a onClick={connexionSettings()}></a>
        </div>
    )
}

export default Connexion;